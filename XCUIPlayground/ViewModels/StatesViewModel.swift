import SwiftUI
import Combine

final class StatesViewModel: ObservableObject {
    let title = String(localized: "StatesView.title")
    let descriptionText = String(localized: "StatesView.description")
}
